{
  "name": "aws-ddd-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:ts && npm_config_cache=.npm-cache sam build --template-file template.yaml",
    "build:ts": "tsc && node script/prepare-dist.cjs",
    "test": "jest",
    "validate": "sam validate --template-file template.yaml",
    "pretest": "npm run build:ts",
    "local:api:warm": "npm run build:ts && npm_config_cache=.npm-cache sam build --template-file template.yaml && sam local start-api --template .aws-sam/build/template.yaml --env-vars env.json --warm-containers EAGER",
    "secrets:dev": "node script/create-github-dev-secrets-script.cjs && bash script/set-github-dev-secrets.local.sh",
    "deploy:dev": "npm run secrets:dev && git commit --allow-empty -m \"chore: redeploy to sync secrets\" && git push"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1036.0",
    "axios": "^1.15.2",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "lambda-multipart-parser": "^1.0.1",
    "mongoose": "^9.5.0",
    "nodemailer": "^8.0.5",
    "twilio": "^6.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.12.2",
    "@types/nodemailer": "^8.0.0",
    "jest": "^29.7.0",
    "typescript": "^5.9.3"
  }
}
